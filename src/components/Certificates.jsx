import React from 'react'

const Certificates = (props) => {
    return (
        <div>
            <h1>Certificate</h1>
            <p>{props.certificates}</p>
        </div>
    )
}

export default Certificates     